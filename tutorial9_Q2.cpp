#include <iostream>
using namespace std;

/*Assume that you have a classroom (only 1 classroom) and you have several
requests from lecturers to use the classroom (Lecture 7, Slide No 8). Apply the
interval scheduling problem to schedule as many requests as possible to
optimise the usage of the classroom. Implement your solution using:
a. Shortest interval; and
b. Fewest conflicts*/

int main() {

    
    return 0;
}